import { UserFormatPipe } from './user-format.pipe';

describe('UserFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
